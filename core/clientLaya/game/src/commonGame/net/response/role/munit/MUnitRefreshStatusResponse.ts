namespace Shine
{
	/** 刷新主角状态(generated by shine) */
	export class MUnitRefreshStatusResponse extends GameResponse
	{
		/** 数据类型ID */
		public static dataID:number=GameResponseType.MUnitRefreshStatus;
		
		/** 单位序号 */
		public index:number;
		
		/** 改变的状态组 */
		public status:SMap<number,boolean>;
		
		constructor()
		{
			super();
			this._dataID=GameResponseType.MUnitRefreshStatus;
		}
		
		/** 获取数据类名 */
		public getDataClassName():string
		{
			return "MUnitRefreshStatusResponse";
		}
		
		/** 读取字节流(简版) */
		protected toReadBytesSimple(stream:BytesReadStream):void
		{
			super.toReadBytesSimple(stream);
			
			this.index=stream.readInt();
			
			var statusLen:number=stream.readLen();
			if(this.status!=null)
			{
				this.status.clear();
				this.status.ensureCapacity(statusLen);
			}
			else
			{
				this.status=new SMap<number,boolean>(statusLen);
			}
			
			var statusT:SMap<number,boolean>=this.status;
			for(var statusI:number=statusLen-1;statusI>=0;--statusI)
			{
				var statusK:number;
				var statusV:boolean;
				statusK=stream.readInt();
				
				statusV=stream.readBoolean();
				
				statusT.put(statusK,statusV);
			}
			
		}
		
		/** 执行 */
		protected execute():void
		{
			
		}
		
	}
}
