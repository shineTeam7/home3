namespace Shine
{
	/** 角色事务组数据(generated by shine) */
	export class PlayerWorkListData extends BaseData
	{
		/** 数据类型ID */
		public static dataID:number=BaseDataType.PlayerWorkList;
		
		/** 数据组 */
		public list:SList<PlayerWorkData>;
		
		constructor()
		{
			super();
			this._dataID=BaseDataType.PlayerWorkList;
		}
		
		/** 获取数据类名 */
		public getDataClassName():string
		{
			return "PlayerWorkListData";
		}
		
		/** 读取字节流(简版) */
		protected toReadBytesSimple(stream:BytesReadStream):void
		{
			var listLen:number=stream.readLen();
			if(this.list!=null)
			{
				this.list.clear();
				this.list.ensureCapacity(listLen);
			}
			else
			{
				this.list=new SList<PlayerWorkData>();
			}
			
			var listT:SList<PlayerWorkData>=this.list;
			for(var listI:number=listLen-1;listI>=0;--listI)
			{
				var listV:PlayerWorkData;
				listV=stream.readDataSimpleNotNull() as PlayerWorkData;
				
				listT.add(listV);
			}
			
		}
		
		/** 写入字节流(简版) */
		protected toWriteBytesSimple(stream:BytesWriteStream):void
		{
			stream.writeLen(this.list.size());
			if(!this.list.isEmpty())
			{
				for(var listVI=0,listVLen=this.list.length;listVI<listVLen;++listVI)
				{
					var listV:PlayerWorkData=this.list[listVI];
					stream.writeDataSimpleNotNull(listV);
					
				}
			}
			
		}
		
		/** 复制(潜拷贝) */
		protected toShadowCopy(data:BaseData):void
		{
			if(!(data instanceof PlayerWorkListData))
				return;
			
			var mData:PlayerWorkListData=data as PlayerWorkListData;
			
			this.list=mData.list;
		}
		
		/** 复制(深拷贝) */
		protected toCopy(data:BaseData):void
		{
			if(!(data instanceof PlayerWorkListData))
				return;
			
			var mData:PlayerWorkListData=data as PlayerWorkListData;
			
			if(this.list!=null)
			{
				this.list.clear();
				this.list.ensureCapacity(mData.list.size());
			}
			else
			{
				this.list=new SList<PlayerWorkData>();
			}
			
			var listT:SList<PlayerWorkData>=this.list;
			if(!mData.list.isEmpty())
			{
				for(var listVI=0,listVLen=mData.list.length;listVI<listVLen;++listVI)
				{
					var listV:PlayerWorkData=mData.list[listVI];
					var listU:PlayerWorkData;
					listU=listV.clone() as PlayerWorkData;
					
					listT.add(listU);
				}
			}
			
		}
		
		/** 初始化初值 */
		public initDefault():void
		{
			this.list=new SList<PlayerWorkData>();
		}
		
	}
}
