package com.home.commonManager.net.serverResponse.login.system;
import com.home.commonBase.constlist.generate.ServerMessageType;
import com.home.commonBase.data.login.LoginInitServerData;
import com.home.commonBase.data.system.ServerInfoData;
import com.home.commonBase.global.BaseC;
import com.home.commonManager.global.ManagerC;
import com.home.commonManager.net.serverRequest.login.ReBeLoginToManagerServerRequest;
import com.home.commonManager.net.serverResponse.login.base.LoginToManagerServerResponse;
import com.home.shine.bytes.BytesReadStream;
import com.home.shine.constlist.SocketType;
import com.home.shine.ctrl.Ctrl;
import com.home.shine.support.DataWriter;
import com.home.shine.support.pool.DataPool;

/** (generated by shine) */
public class BeLoginToManagerServerResponse extends LoginToManagerServerResponse
{
	/** 数据类型ID */
	public static final int dataID=ServerMessageType.BeLoginToManager;
	
	public int id;
	
	/** 是否第一次连接 */
	public boolean isFirst;
	
	public BeLoginToManagerServerResponse()
	{
		_dataID=ServerMessageType.BeLoginToManager;
	}
	
	/** 获取数据类名 */
	@Override
	public String getDataClassName()
	{
		return "BeLoginToManagerServerResponse";
	}
	
	/** 读取字节流(简版) */
	@Override
	protected void toReadBytesSimple(BytesReadStream stream)
	{
		super.toReadBytesSimple(stream);
		
		this.id=stream.readInt();
		
		this.isFirst=stream.readBoolean();
		
	}
	
	/** 转文本输出 */
	@Override
	protected void toWriteDataString(DataWriter writer)
	{
		super.toWriteDataString(writer);
		
		writer.writeTabs();
		writer.sb.append("id");
		writer.sb.append(':');
		writer.sb.append(this.id);
		
		writer.writeEnter();
		writer.writeTabs();
		writer.sb.append("isFirst");
		writer.sb.append(':');
		writer.sb.append(this.isFirst);
		
		writer.writeEnter();
	}
	
	/** 执行 */
	@Override
	protected void execute()
	{
		if(!ManagerC.main.hasLoginServer(id))
		{
			Ctrl.throwError("找不到登录服配置",id);
			return;
		}
		
		ManagerC.server.getSocketInfo(SocketType.Login).registerSocket(id,socket);
		
		LoginInitServerData initData=isFirst ? ManagerC.main.createLoginInitData(id) : null;
		
		socket.send(ReBeLoginToManagerServerRequest.create(initData));
	}
	
	/** 回池 */
	@Override
	protected void toRelease(DataPool pool)
	{
		super.toRelease(pool);
		
		this.id=0;
		this.isFirst=false;
	}
	
}
