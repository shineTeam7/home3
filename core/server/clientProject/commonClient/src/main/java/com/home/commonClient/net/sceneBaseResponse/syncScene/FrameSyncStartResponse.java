package com.home.commonClient.net.sceneBaseResponse.syncScene;
import com.home.commonBase.data.scene.fight.FrameSyncData;
import com.home.commonClient.constlist.generate.GameResponseType;
import com.home.commonClient.constlist.generate.SceneBaseResponseType;
import com.home.commonClient.net.sceneBaseResponse.base.SceneSResponse;
import com.home.shine.bytes.BytesReadStream;
import com.home.shine.data.BaseData;
import com.home.shine.support.DataWriter;
import com.home.shine.support.collection.SList;
import com.home.shine.support.pool.DataPool;

/** 帧同步起始消息(generated by shine) */
public class FrameSyncStartResponse extends SceneSResponse
{
	/** 数据类型ID */
	public static final int dataID=SceneBaseResponseType.FrameSyncStart;
	
	/** 数据组 */
	public SList<FrameSyncData> list;
	
	public FrameSyncStartResponse()
	{
		_dataID=SceneBaseResponseType.FrameSyncStart;
	}
	
	/** 获取数据类名 */
	@Override
	public String getDataClassName()
	{
		return "FrameSyncStartResponse";
	}
	
	/** 执行 */
	@Override
	protected void execute()
	{
		
	}
	
	/** 读取字节流(完整版) */
	@Override
	protected void toReadBytesFull(BytesReadStream stream)
	{
		super.toReadBytesFull(stream);
		
		stream.startReadObj();
		
		int listLen=stream.readLen();
		if(this.list!=null)
		{
			this.list.clear();
			this.list.ensureCapacity(listLen);
		}
		else
		{
			this.list=new SList<FrameSyncData>(FrameSyncData[]::new,listLen);
		}
		
		SList<FrameSyncData> listT=this.list;
		for(int listI=listLen-1;listI>=0;--listI)
		{
			FrameSyncData listV;
			BaseData listVT=stream.readDataFullNotNull();
			if(listVT!=null)
			{
				if(listVT instanceof FrameSyncData)
				{
					listV=(FrameSyncData)listVT;
				}
				else
				{
					listV=new FrameSyncData();
					if(!(listVT.getClass().isAssignableFrom(FrameSyncData.class)))
					{
						stream.throwTypeReadError(FrameSyncData.class,listVT.getClass());
					}
					listV.shadowCopy(listVT);
				}
			}
			else
			{
				listV=null;
			}
			
			listT.add(listV);
		}
		
		stream.endReadObj();
	}
	
	/** 读取字节流(简版) */
	@Override
	protected void toReadBytesSimple(BytesReadStream stream)
	{
		super.toReadBytesSimple(stream);
		
		int listLen=stream.readLen();
		if(this.list!=null)
		{
			this.list.clear();
			this.list.ensureCapacity(listLen);
		}
		else
		{
			this.list=new SList<FrameSyncData>(FrameSyncData[]::new,listLen);
		}
		
		SList<FrameSyncData> listT=this.list;
		for(int listI=listLen-1;listI>=0;--listI)
		{
			FrameSyncData listV;
			listV=(FrameSyncData)stream.readDataSimpleNotNull();
			
			listT.add(listV);
		}
		
	}
	
	/** 转文本输出 */
	@Override
	protected void toWriteDataString(DataWriter writer)
	{
		super.toWriteDataString(writer);
		
		writer.writeTabs();
		writer.sb.append("list");
		writer.sb.append(':');
		writer.sb.append("List<FrameSyncData>");
		if(this.list!=null)
		{
			SList<FrameSyncData> listT=this.list;
			int listLen=listT.size();
			writer.sb.append('(');
			writer.sb.append(listLen);
			writer.sb.append(')');
			writer.writeEnter();
			writer.writeLeftBrace();
			for(int listI=0;listI<listLen;++listI)
			{
				FrameSyncData listV=listT.get(listI);
				writer.writeTabs();
				writer.sb.append(listI);
				writer.sb.append(':');
				if(listV!=null)
				{
					listV.writeDataString(writer);
				}
				else
				{
					writer.sb.append("FrameSyncData=null");
				}
				
				writer.writeEnter();
			}
			writer.writeRightBrace();
		}
		else
		{
			writer.sb.append("=null");
		}
		
		writer.writeEnter();
	}
	
	/** 回池 */
	@Override
	protected void toRelease(DataPool pool)
	{
		super.toRelease(pool);
		
		this.list=null;
	}
	
}
